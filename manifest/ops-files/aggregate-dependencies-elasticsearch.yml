- type: replace
  path: /instance_groups/name=zipkin/jobs/-
  value:
    consumes:
      elasticsearch:
        from: ((elasticsearch-from))
        deployment: ((elasticsearch-deployment))
    name: aggregate-dependencies
    release: zipkin
    lifecycle: errand
    properties:
      zipkin_dependencies:
        days_to_look_back: "2 days ago"
